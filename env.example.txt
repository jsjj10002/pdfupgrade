# 이 파일을 .env로 복사하여 사용하세요
# cp env.example.txt .env

# 애플리케이션 환경
APP_ENV=development
APP_NAME=pdfupgrade
APP_VERSION=1.0.0
DEBUG=true

# API 서버 설정
API_HOST=0.0.0.0
API_PORT=8000
API_BASE_URL=http://localhost:8000

# CORS 설정
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# 스토리지 경로
STORAGE_DIR=./storage
MODELS_DIR=./app/models
LOGS_DIR=./logs

# GPU 설정
USE_GPU=auto  # auto|true|false
CUDA_VISIBLE_DEVICES=0

# Redis 설정
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=

# Celery 설정
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
CELERY_MAX_WORKERS=2
CELERY_TASK_TIME_LIMIT=3600  # 초 단위

# 파일 처리 제한
MAX_FILE_SIZE_MB=250
MAX_FILES_PER_JOB=10
SUPPORTED_FILE_TYPES=application/pdf

# 업스케일 설정
UPSCALE_ENABLED=true
UPSCALE_DEFAULT_SCALE=2  # 2 또는 4
UPSCALE_FACE_ENHANCE=false
UPSCALE_MODEL_NAME=RealESRGAN_x2plus

# 워터마크 제거 설정
WATERMARK_ENABLED=true
WATERMARK_DEFAULT_MODE=auto  # auto|template|segment
WATERMARK_DETECTION_THRESHOLD=0.8

# OCR 설정
OCR_ENABLED=true
OCR_ENGINE=paddle  # paddle|tesseract
OCR_DEFAULT_LANGS=kor+eng
OCR_USE_ANGLE_CLS=true
OCR_MIN_CONFIDENCE=0.5

# PDF 생성 설정
PDF_DPI=300
PDF_OUTPUT_PDFA=false
PDF_COMPRESSION=true

# 로깅 설정
LOG_LEVEL=INFO  # DEBUG|INFO|WARNING|ERROR|CRITICAL
LOG_FORMAT=json  # json|text
LOG_ROTATION=true
LOG_MAX_SIZE_MB=100

# 보안 설정
SECRET_KEY=your-secret-key-here-change-in-production
ADMIN_API_KEY=

# 모니터링 (향후)
SENTRY_DSN=
PROMETHEUS_ENABLED=false

